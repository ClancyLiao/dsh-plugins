# Bundle layer for dsh-client-ui-composer-optimize.
# Applied when a profile lists this bundle (dsh plugin add <pkg>).
- insert:
    - id: ui-composer-optimize
      name: dsh-client-ui-composer-optimize
